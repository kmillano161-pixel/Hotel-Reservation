# Admin Design Plan

**Information:**
- AddRoom.jsx: Form with inline styles (fields, image preview)
- Bookings.jsx: List with inline styles
- Both use style={{}}, not responsive

**Plan:**
1. Add classNames (admin-container, admin-form, form-field, etc.)
2. App.css admin section (cards, grid forms, responsive table)
3. Replace inline styles with classes
4. Mobile responsive (stack fields, scroll table)

✅ Step 1: App.css admin styles added
✅ Step 2: AddRoom.jsx classes updated
- [ ] Edit Bookings.jsx replace styles
- [ ] Test /admin/add-room, /admin/bookings

Proceed?

